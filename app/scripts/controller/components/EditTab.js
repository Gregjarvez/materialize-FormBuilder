import React from "react";import brace from 'brace';import AceEditor from "react-ace";import 'brace/mode/css';import 'brace/theme/github';class Editor extends React.Component {    constructor(props) {        super(props)        this.state = {placeholder: `.${this.props.classname} {}`}    }    shouldComponentUpdate() {        return false;    }    render() {        return (            <AceEditor                mode="css"                theme="github"                name="react-ace-editor"                onChange={this.props.onEditorChange}                defaultValue={this.state.placeholder}                fontSize={16}                height="400px"                width="100%"                style={{border: "10px solid rgba(31, 71, 136, 0.3)", margin: "10px auto"}}            />        )    }}class EditTab extends React.Component {    constructor(props) {        super(props)        this.state = {            defaultHasActive: true,            advancedHasActive: false,            editorObj: "",        }        this.onEditorChange = this.onEditorChange.bind(this);    }    makeActive(states) {        let [active, inactive] = states;        return this.setState({            [active]: true,            [inactive]: false        })    }    handleSubmit(e) {        return e.preventDefault()    }    onEditorChange(value) {        let style = value;        return this.setState({editorObj: style});    }    render() {        let defaultIsActive = `${(this.state.defaultHasActive && "setting--is-active")}`        let advancedIsActive = `${(this.state.advancedHasActive && "setting--is-active")}`        let states = ["defaultHasActive", "advancedHasActive"]        return (            <div className="factory--editTap">                <div className="setting">                    <div                        className={`setting--default ${defaultIsActive}`}                        onClick={() => this.makeActive(states)}                    >Default                    </div>                    <div                        className={`setting--default ${advancedIsActive}`}                        onClick={(e) => this.makeActive(states.reverse())}>Advanced                    </div>                </div>                {                    this.state.defaultHasActive &&                    <form className="factory--forms editor" onSubmit={(e) => this.handleSubmit(e)}>                        {this.props.children}                    </form>                }                {                    this.state.advancedHasActive &&                    <div className="factory--forms" id="react-ace-editor">                        <div className="factory--side-note">                            Styles are not saved. Navigating from this                            Tab Before saving will render all style definitions lost. :)                        </div>                        <Editor onEditorChange={this.onEditorChange} classname={this.props.className}/>                        <div className="setting--button setting--button-centered"                              onClick={() => this.props.emitClass(this.state.editorObj)}>                            Save                        </div>                    </div>                }                          </div>        );    }}export default EditTab;